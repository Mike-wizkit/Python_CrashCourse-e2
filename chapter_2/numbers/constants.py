# an constant is like a variable that stays the same trough the whole program to make a constant
# you have to write  the variable in ALL CAPS that way it indictes that its a constant and
# shouldn't be changed
MAX_CONNECTIONS = 5000
